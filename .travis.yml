language: python

notifications:
  email: false

python:
  - 2.7
  - 3.5

install:
  - pip install tox-travis
  - pip install pytest
  - pip install pytest-cov
  - pip install .

script:
  - python test.py
  - python setup.py check --strict --metadata
  - tox

after_success:
  - coveralls
